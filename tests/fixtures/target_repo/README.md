# Offline prompt target fixture

This tiny repository is copied into a temporary Git repository by the offline
integration tests.  It intentionally contains a real renderer, a production
Pydantic schema, and a production call assembly function.
